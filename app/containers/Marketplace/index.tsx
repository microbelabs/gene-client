import * as React from 'react';
import styled from 'styled-components';

const Fragment = styled.div ``;

export default class Marketplace extends React.Component {
  render() {
    return (
      <Fragment>Marketplace</Fragment>
    );
  }
}
