import * as React from 'react';
import styled from 'styled-components';

const Fragment = styled.div ``;

export default class Games extends React.Component {
  render() {
    return (
      <Fragment>Games</Fragment>
    );
  }
}
